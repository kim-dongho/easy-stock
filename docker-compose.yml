services:
  # Go API 서버 (DB 읽기 전용)
  server:
    build: ./apps/server
    ports:
      - "5051:5051"
    environment:
      - PORT=5051
      # 기존 quant-platform DB 공유 (SELECT only)
      - DATABASE_URL=postgres://user:password@host.docker.internal:15432/quant?sslmode=disable
